package com.example.litechat.listeners

class ListenerObject
{
    var listener: CustomListener? = null

    fun setCustomObjectListener(listener: CustomListener) {

        this.listener = listener
    }
}